# Contributing

感谢参与 Astcho。请从小而清晰的改动开始，并确保：

1. 不提交密钥、QQ 号、聊天记录、人格画像、数据库或表情图片。
2. 新功能使用 `Settings`、运行时容器和既有存储边界，不在导入时连接模型或写数据。
3. 管理、删除和诊断能力必须经过管理员权限检查。
4. 为行为变化补充测试，并运行 `ruff check src tests` 与 `pytest`。

提交信息建议使用简短的祈使句。安全问题请不要在公开 Issue 中附带真实凭据或私人数据。

